//write a c program to check wheather no. is even or odd,print yes if no. is even otherwise odd

#include <stdio.h>
int main(){
	int c;
	scanf("%d",&c);
	if(c%2==0)
	{
	
	printf("%d is even",c);
	}
	else{
		printf("%d is odd",c);
		}		
}

